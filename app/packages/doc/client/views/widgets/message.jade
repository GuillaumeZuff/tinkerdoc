template(name="docWidget_message")
    div(class=cardClasses)
        div.card-content.white-text
            if card.title
                div.card-title {{_ card.title}}
            if card.caption
                {{_ card.caption}}
